#!/usr/bin/python
#Classes to access SQL database that contains monitoring data
#@Gregorius 2016

#CURRENT PROGRESS : only class to insert data
#TODO : read data

import MySQLdb

class dbaccess(object):

    #define class to simulate a simple calculator

    def __init__ (self):
	#Database connection
	servername = "localhost"
	username = "root"
	password = "12345678"
	dbname = "testdb"
	# Open database connection
	db = MySQLdb.connect(servername,username,password,dbname)
	# prepare a cursor object using cursor() method
	cursor = db.cursor()

    def insert(self,type,data):
	
	q = """CREATE TABLE IF NOT EXISTS nfm (
        	data  VARCHAR(50),
         	data2  VARCHAR(15),
	        ))""" % (d,)
	cursor.execute(q)

        #Insert one data based on type (nfm/hum/rpm)
	try:
		# Execute the SQL command
		cursor.execute("INSERT INTO "+type+" (data) VALUES ("+data+")");
		# Commit your changes in the database
		db.commit()
	except:
		# Rollback in case there is any error
		db.rollback()

	return 

    def getCurrent(self):

        return self.current


# disconnect from server
db.close()
